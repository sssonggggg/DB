CREATE FUNCTION [DBO].[GETDT]
      (  @DT udtDate )
RETURNS CHAR(10)

AS
BEGIN
    DECLARE @DTCHAR VARCHAR(300)
    SET @DTCHAR = (CASE WHEN @DT != '' AND @DT IS NOT NULL THEN CONVERT(CHAR(10), CONVERT (DATE, @DT )) ELSE ''END)
    RETURN @DTCHAR
END
